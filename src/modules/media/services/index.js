/**
 * Export services
 */
const mediaService = require('./media.service');
const optimizationService = require('./optimization.service');

module.exports = {
  mediaService,
  optimizationService
}; 