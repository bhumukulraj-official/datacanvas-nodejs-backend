/**
 * Export validators
 */
const searchValidator = require('./search.validator');
const analyticsValidator = require('./analytics.validator');

module.exports = {
  searchValidator,
  analyticsValidator
}; 